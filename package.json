{
  "name": "paycodes",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix client",
    "dev": "vercel dev"
  },
  "dependencies": {
    "dotenv": "^16.4.1",
    "mongoose": "^7.6.0"
  }
}
